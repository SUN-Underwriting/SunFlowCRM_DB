# =================================================================
# Application
# =================================================================
NEXT_PUBLIC_APP_NAME="SunFlowCRM"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_API_DOMAIN="http://localhost:3000"


# =================================================================
# Authentication Provider
# =================================================================
# Choose 'supertokens' (default, self-hosted) or 'stack' (Stack Auth)
AUTH_PROVIDER="supertokens"
NEXT_PUBLIC_AUTH_PROVIDER="supertokens"


# =================================================================
# SuperTokens Configuration (used when AUTH_PROVIDER=supertokens)
# =================================================================
# Self-hosted SuperTokens Core URL
SUPERTOKENS_CONNECTION_URI="http://localhost:3567"

# Generate with: openssl rand -hex 32
SUPERTOKENS_API_KEY=


# =================================================================
# Stack Auth Configuration (used when AUTH_PROVIDER=stack)
# =================================================================
# Obtain from Stack Auth dashboard at http://localhost:8101 (local)
# or https://app.stack-auth.com (cloud)
NEXT_PUBLIC_STACK_PROJECT_ID=
NEXT_PUBLIC_STACK_PUBLISHABLE_CLIENT_KEY=
STACK_SECRET_SERVER_KEY=

# Stack Auth server URLs (change for cloud deployment)
NEXT_PUBLIC_STACK_URL="http://localhost:8101"
NEXT_PUBLIC_STACK_API_URL="http://localhost:8102"


# =================================================================
# Database (PostgreSQL)
# =================================================================
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/sun_uw"


# =================================================================
# Redis (BullMQ job queues)
# =================================================================
REDIS_URL="redis://localhost:6379"
# REDIS_PASSWORD is required in production (docker-compose.prod.yml)
REDIS_PASSWORD=


# =================================================================
# Internal Worker Secret
# =================================================================
# Shared secret between the Next.js app and the notifications worker.
# REQUIRED in production — generate with: openssl rand -hex 32
INTERNAL_WORKER_SECRET=


# =================================================================
# Error Tracking (Sentry) — Optional
# =================================================================
# Create a project at https://sentry.io and fill in the values below.

# Found at: Settings > Projects > [Your Project] > Client Keys (DSN)
NEXT_PUBLIC_SENTRY_DSN=

# Found at: Settings > Organization > General Settings
NEXT_PUBLIC_SENTRY_ORG=
NEXT_PUBLIC_SENTRY_PROJECT=

# Generate at: https://sentry.io/settings/account/api/auth-tokens/
SENTRY_AUTH_TOKEN=

# Set to "true" to disable Sentry (e.g. in development)
NEXT_PUBLIC_SENTRY_DISABLED="true"


# =================================================================
# Important Notes
# =================================================================
# 1. Copy this file to '.env' (local dev) or '.env.local' (Next.js)
# 2. Never commit the actual .env file to version control
# 3. Replace all placeholder values with real secrets
# 4. For Docker production deployment see docker-compose.prod.yml
