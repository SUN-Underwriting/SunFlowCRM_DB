# PR#1 Testing Checklist: Deals Board Toolbar + Filters

## Изменённые файлы

- ✅ **NEW** `src/features/crm/deals/components/deals-board-toolbar.tsx`
- ✅ **NEW** `src/hooks/use-debounce.ts`
- ✅ **MODIFIED** `src/app/dashboard/crm/deals/page.tsx`

## Функциональное тестирование

### 1. Search (поиск)

- [ ] Открыть `/dashboard/crm/deals`
- [ ] Ввести текст в поле поиска
- [ ] Проверить что поиск работает с debounce (300ms)
- [ ] Проверить поиск по:
  - [ ] Названию сделки (title)
  - [ ] Имени контакта (person firstName/lastName)
  - [ ] Названию организации (organization name)
- [ ] Проверить что URL обновляется параметром `?q=...`
- [ ] Скопировать URL с поиском и открыть в новой вкладке — фильтр должен сохраниться
- [ ] Очистить поиск через чип с крестиком
- [ ] Очистить поиск кнопкой "Reset"

### 2. Owner Filter (фильтр по владельцу)

- [ ] Выбрать владельца из выпадающего списка
- [ ] Проверить что показываются только сделки этого владельца
- [ ] Проверить что URL обновляется параметром `?owner=...`
- [ ] Проверить что появляется чип "Owner: [Name]"
- [ ] Удалить фильтр через крестик на чипе
- [ ] Переключиться на "All Owners"

### 3. Status Filter (фильтр по статусу)

- [ ] Выбрать статус (OPEN/WON/LOST)
- [ ] Проверить что показываются только сделки с этим статусом
- [ ] Проверить что URL обновляется параметром `?status=...`
- [ ] Проверить что появляется чип "Status: [STATUS]"
- [ ] Удалить фильтр через крестик на чипе
- [ ] Переключиться на "All Statuses"

### 4. Combined Filters (комбинация фильтров)

- [ ] Применить поиск + owner filter
- [ ] Применить поиск + status filter
- [ ] Применить owner + status
- [ ] Применить все три фильтра одновременно
- [ ] Проверить что счётчик active filters правильный
- [ ] Проверить что кнопка "Reset (N)" показывает правильное число
- [ ] Нажать "Reset" — все фильтры должны очиститься

### 5. Results Display

- [ ] Проверить что показывается "Showing X of Y deals" когда есть фильтры
- [ ] Проверить что сообщение скрывается когда фильтры сняты
- [ ] Проверить что доска корректно обновляется при изменении фильтров
- [ ] Проверить что пустые колонки показывают "Drop deals here"

### 6. URL Synchronization

- [ ] Применить фильтры и скопировать URL
- [ ] Открыть URL в новой вкладке
- [ ] Проверить что все фильтры применены
- [ ] Проверить что поиск, owner и status соответствуют URL
- [ ] Изменить фильтры и проверить что URL обновляется
- [ ] Использовать браузерные кнопки "назад/вперёд" — фильтры должны сохраняться

## UI/UX тестирование

### Desktop (1920x1080)

- [ ] Toolbar отображается корректно
- [ ] Все контролы видны в одной строке
- [ ] Чипы не перекрывают друг друга
- [ ] Sticky positioning работает при скролле

### Tablet (768x1024)

- [ ] Toolbar адаптируется под ширину
- [ ] Селекты и кнопки не слипаются
- [ ] Чипы переносятся на новую строку

### Mobile (375x667)

- [ ] Контролы располагаются в колонку (flex-col)
- [ ] Поиск занимает всю ширину
- [ ] Селекты занимают всю ширину
- [ ] Кнопка Reset видна

## Performance тестирование

- [ ] Поиск не лагает при быстром вводе (debounce работает)
- [ ] Фильтрация работает плавно при большом количестве сделок (>50)
- [ ] URL не обновляется на каждый символ (только после debounce)
- [ ] Переключение фильтров происходит мгновенно (client-side)

## Edge Cases

### Пустые состояния

- [ ] Поиск без результатов — показываются пустые колонки
- [ ] Нет владельцев в системе — селект Owner не показывается
- [ ] Нет сделок вообще — toolbar всё равно работает

### Некорректные URL параметры

- [ ] Открыть URL с несуществующим owner ID — игнорируется
- [ ] Открыть URL с некорректным status — игнорируется
- [ ] Открыть URL с пустым `?q=` — корректно обрабатывается

### Граничные значения

- [ ] Очень длинный поисковый запрос (>100 символов)
- [ ] Специальные символы в поиске (&, %, =, etc.)
- [ ] Unicode символы (кириллица, эмодзи)

## Accessibility

- [ ] Можно перемещаться по контролам с помощью Tab
- [ ] Enter в поле поиска применяет фильтр
- [ ] Escape очищает активное поле
- [ ] Screen reader правильно читает лейблы
- [ ] Чипы доступны с клавиатуры (можно удалить через Enter/Space)

## Browser Compatibility

- [ ] Chrome (последняя версия)
- [ ] Firefox (последняя версия)
- [ ] Safari (последняя версия)
- [ ] Edge (последняя версия)

## Regression Testing

- [ ] Pipeline selector всё ещё работает
- [ ] Drag & drop сделок всё ещё работает
- [ ] Клик по карточке открывает detail sheet
- [ ] Кнопка "Add Deal" работает
- [ ] Другие страницы CRM не сломались

## Final Checks

- [ ] Нет console errors в браузере
- [ ] Нет TypeScript errors
- [ ] Нет ESLint warnings
- [ ] Performance: lighthouse score не упал
- [ ] Bundle size: не увеличился значительно (проверить с `npm run build`)

## Sign-off

- [ ] Функциональность полностью работает
- [ ] UI соответствует дизайну/требованиям
- [ ] Код ревью пройдено
- [ ] Документация обновлена
- [ ] Ready to merge

---

## Известные ограничения (для Phase 2)

1. Фильтрация происходит client-side (не масштабируется для >1000 deals)
2. Owner filter не поддерживает multiple selection (только один owner)
3. Нет advanced фильтров (value range, expected close date, etc.)
4. Нет saved views (будет в Phase 2)
5. Нет экспорта отфильтрованных результатов
